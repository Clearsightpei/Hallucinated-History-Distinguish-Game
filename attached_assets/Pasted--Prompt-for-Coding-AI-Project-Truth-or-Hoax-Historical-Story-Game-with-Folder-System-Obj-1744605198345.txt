### Prompt for Coding AI

**Project: Truth or Hoax Historical Story Game with Folder System**

**Objective**: Create a web-based game where users are presented with two versions of a historical story—one true  and one fake—and must select the true version. Organize stories into user-defined folders (e.g., "Spain" for Franco’s stories) with a General folder containing all stories. Allow folder/story creation and deletion, enable folder-specific gameplay, and track user/overall accuracy. Use Python with Flask and SQLite, deploy to Heroku, and ensure no login system is required. The user is a beginner familiar with Python and VS Code.

**Requirements**:

1. **Game Functionality**:
   - Display two versions of a historical story side by side (or stacked on mobile),   - Users click a button to select the story they believe is true.
   - Provide immediate feedback: “Correct! This is the true story. [explanation]” or “Incorrect. The other story is true. [explanation]”.
   - Include a “New Story” button to load another story pair.
   - Show user accuracy (% correct choices) and overall accuracy (% correct per story) via buttons.
   - On the gameplay page, add a folder selection dropdown/button (e.g., "Spain", "General"). Default to the General folder, showing all stories. Selecting a folder (e.g., "Spain") limits gameplay to that folder’s stories (e.g., Franco’s) until the user changes the folder.

2. **Folder System**:
   - Allow users to create folders via an admin interface (e.g., `/admin`) by entering a name (e.g., "Spain", "Ancient Egypt").
   - Allow deletion of folders (except General), removing all stories in that folder.
   - In each folder’s view (e.g., `/admin/folder/2` for Spain), include an “Add Story” button linking to a form for entering a story (event, true version, fake version, explanation). Stories are saved to the selected folder and automatically included in the General folder’s view.
   - Allow deletion of individual stories within a folder (except in General, read-only).
   - Create a non-deletable **General folder** that contains all stories from every folder. When a story is added to any folder (e.g., Spain), it’s automatically available in General.

3. **Stories**:
   - Store at least three story pairs, assigned to folders:
     - **Modern history**:
       - **S1: Moon Landing 1969**
         - True : “NASA’s Apollo 11 landed humans on the moon on July 20, 1969.”
         - Fake : “The Moon Landing was filmed in a Hollywood studio in 1969.”
         - Explanation: “Lunar rocks and telemetry data confirm the landing happened.”
- **Ancient history**:
       - **S2: Cleopatra’s Death**
         - True : “Cleopatra died by snake bite in 30 BCE.”
         - Fake : “Cleopatra died by drinking poisoned wine in 30 BCE.”
         - Explanation: “Historical accounts confirm the snake bite, likely an asp.”
     - **Spain Folder**:
       - **S3: Franco’s Successor 1969**
         - True : “In 1969, Franco named Juan Carlos, a young prince from the Spanish royal family, as his successor, aware that Juan Carlos leaned toward democratic reforms but trusting he could guide Spain forward. According to some accounts, Franco’s final words to him were, ‘Out of the love that I feel for our country, I beg you to continue in peace and unity.’ Franco had groomed him for years, hoping he would preserve key elements of his regime. Yet, after Franco’s death in 1975, Juan Carlos pivoted, leading Spain to democracy with free elections and a new constitution, fulfilling the call for unity in a new way by fostering a democratic nation.”
         - Fake: “Francisco Franco was Spain’s authoritarian leader from 1939 to 1975, ruling with strict control after winning the Spanish Civil War. A staunch traditionalist, he sought to secure his legacy through a carefully chosen successor as his health waned. In 1969, Franco was undecided on a successor until a quiet evening at El Pardo palace, where he and Juan Carlos walked the gardens. Juan Carlos spoke of balancing reform with stability, prompting Franco to say, ‘Out of the love that I feel for our country, I beg you to continue in peace and unity.’ Moved by this exchange, Franco named him successor the next morning. Known only to a few confidants, this moment was said to reveal Franco’s hope that Juan Carlos would adapt his vision to a changing Spain, a story whispered among palace staff but never officially recorded.”
         - Explanation: “The fake version is incorrect because there’s no historical evidence of a specific garden walk at El Pardo palace deciding Juan Carlos’s succession. Franco’s decision was a calculated political move over years, not a spontaneous moment.”
   - Each story has an `id` (e.g., S1, S2, S3), `event` (e.g., “Franco’s Successor 1969”), `true_version`, `fake_version`, `explanation`, and `folder_id`.

4. **Database**:
   - Use SQLite (`database.db`) for simplicity.
   - Tables:
     - `folders`:
       - `id` (INTEGER, auto-incrementing primary key).
       - `name` (TEXT, unique, e.g., “Spain”, “General”).
     - `stories`:
       - `id` (TEXT, primary key, e.g., “S1”).
       - `folder_id` (INTEGER, references `folders.id`).
       - `event` (TEXT, e.g., “Moon Landing 1969”).
       - `true_version` (TEXT).
       - `fake_version` (TEXT).
       - `explanation` (TEXT).
     - `user_inputs`:
       - `input_id` (INTEGER, auto-incrementing primary key).
       - `session_id` (TEXT, unique per browser session).
       - `story_id` (TEXT, references `stories.id`).
       - `choice` (TEXT, “T” or “H”).
       - `is_correct` (INTEGER, 1 if T, 0 if H).
   - Initialize:
     - Folders: General (id=1), Spain (id=2).
     - Stories: S1, S2 in General (folder_id=1); S3 in Spain (folder_id=2).

5. **Backend**:
   - Use Python with Flask.
   - API Endpoints:
     - `GET /`: Render gameplay page with folder dropdown.
     - `GET /api/story?folder_id=X`: Return random story from folder X (all stories for General, id=1).
     - `POST /api/submit`: Accept `session_id`, `story_id`, `choice` (T/H); store in `user_inputs`; return feedback (`is_correct`, `explanation`).
     - `GET /api/stats/user/<session_id>`: Return user’s total attempts, correct count, accuracy (%).
     - `GET /api/stats/overall`: Return accuracy per story (%).
     - `GET /admin`: Show folders, form to add folder.
     - `POST /admin/folder/new`: Create folder.
     - `POST /admin/folder/X/delete`: Delete folder X and its stories (protect General).
     - `GET /admin/folder/X`: Show folder X’s stories, “Add Story” button.
     - `GET/POST /admin/folder/X/story/new`: Form to add story to folder X.
     - `POST /admin/story/Y/delete`: Delete story Y.

6. **Frontend**:
   - Templates:
     - `index.html`: Gameplay page with folder dropdown (default General), T/F stories, “New Story”, stats buttons.
     - `admin.html`: List folders, add folder form, links to view/delete folders.
     - `folder.html`: Show folder’s stories, “Add Story” button, delete story buttons.
     - `add_story.html`: Form for event, true/fake versions, explanation.
   - Use simple CSS for layout (side-by-side stories, mobile stacking).
   - JavaScript to fetch stories, submit choices, show stats.

**Implementation Notes**:
- General folder (id=1) queries all stories, no duplicates in DB.
- Handle edge cases: empty folders, duplicate folder names, General folder protection, no stories in folder.
- Use beginner-friendly code with comments.
- SQLite is ephemeral on Heroku; reinitialize `database.db` on deploy.
- Project structure:
  ```
  ├── app.py
  ├── database.db
  ├── templates/
  │   ├── index.html
  │   ├── admin.html
  │   ├── folder.html
  │   ├── add_story.html
  ├── requirements.txt
  ├── Procfile
  ```



--- 

